codecov:
  require_ci_to_pass: yes
  max_report_age: off

coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    project:
      default:
        enabled: no
      frontend:
        target: 70%
        flags:
          - frontend
      backend:
        target: 70%
        flags:
          - backend

flags:
  frontend:
    paths:
      - src/
    carryforward: true
  backend:
    paths:
      - backend/
    carryforward: true

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
